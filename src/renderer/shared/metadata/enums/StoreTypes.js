import { ModelTypes} from '@shared/metadata'

const StoreTypes = {
    PERSONE: 'persone',
    PERSONA_FISICA: ModelTypes.PERSONA_FISICA,
    PERSONA_GIURIDICA: ModelTypes.PERSONA_GIURIDICA,
    PROCEDIMENTO: ModelTypes.PROCEDIMENTO,
    RICERCA: 'ricerca',
}

export default StoreTypes;